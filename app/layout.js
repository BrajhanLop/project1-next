// ESTA PAGINA VIENE A SER EL INDEX.HTML

export default function RootLayout({ children }) {
  return (
    <html>
      <head>
        <title>mi primera web de next</title>
        </head>
      <body>{children}</body>
    </html>
  )
}
